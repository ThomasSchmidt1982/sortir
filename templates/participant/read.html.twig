{% extends 'base.html.twig' %}

{% block body %}
    <h1>Mon profil</h1>
    <div>
        {{ form_start(participantForm) }}
        {{ form_widget(participantForm) }}
        {{ form_end(participantForm) }}
    </div>
{% endblock %}

{% block title %}
    Mon profil | {{ parent() }}
{% endblock %}

